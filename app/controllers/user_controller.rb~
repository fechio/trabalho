class UserController < ApplicationController
  def index
	@users = User.all
  end
  def create
	@users = User.new params[:user]
	if @user.save
		redirect_to :action => 'index'
	else
		render :action => 'new'
  end
end
end
